# Repos a reviewer of THIS repo may search and cite, materialized under
# .siblings/<owner>/<repo>. One per line. The reviewer host must also carry a
# SOURCE_PATHS entry for each, or the sibling reports as `missing`.
#
# plow-hermes-agent is the image this variant is built FROM: it owns the boot
# (plow-init), the env names this image's services read, the inference lane the
# Plow backend calls, and the plugin pin. A review that cannot read it cannot
# tell a correct variant from one that re-owns a base fact.
#
# life-assistant-hermes-agent is the reference variant -- its s6 longruns and
# agent-index reporter are what the ones here are modelled on, so a reviewer can
# diff against a working example instead of asserting a shape from memory.
#
# plow-agents owns the deploy CLI, the image contract and the compose the
# one-click path uses. NousResearch/hermes-agent stays out: pinned by digest in
# the base, never read as source from here.

plow-pbc/plow-hermes-agent
plow-pbc/life-assistant-hermes-agent
plow-pbc/plow-agents
